ADMIN_USER=admin

# Plain password — fine for local dev
ADMIN_PASS=admin

# Hashed password — use in production (takes precedence over ADMIN_PASS)
# Generate with: php -r "echo password_hash('yourpassword', PASSWORD_BCRYPT);"
# ADMIN_PASS_HASH=
